version: '3.8'

services:
  miktex-api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./generated:/app/generated
    environment:
      - NODE_ENV=production
    restart: unless-stopped 